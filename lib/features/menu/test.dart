import 'package:flutter/material.dart';

class JustTestScreen extends StatelessWidget {
  const JustTestScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text('Hi Test'));
  }
}
